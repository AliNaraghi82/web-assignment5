<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Countries</title>
    <link href="/css/main.css" rel="stylesheet" />
  </head>
  <body>
    <%- include('partials/navbar', {page:"/un/countries"}) %>
    <div class="container mx-auto p-4">
      <div class="hero bg-base-200 rounded-lg p-4">
        <div class="hero-content flex-col lg:flex-row">
          <div>
            <h1 class="text-5xl font-bold text-center">Countries</h1>
            <p class="py-6">
              Browse countries, i.e., United Nations member states. Click the
              'region' buttons to filter by regions such as:
              <a
                href="/un/countries?region=africa"
                class="btn btn-primary btn-sm"
                >Africa</a
              >
              <a href="/un/countries?region=asia" class="btn btn-primary btn-sm"
                >Asia</a
              >
            </p>
          </div>
        </div>
      </div>
    </div>
    <% if (countries.length > 0) { %>
    <div class="container mx-auto p-4">
      <table class="w-full table table-zebra">
        <% for (let country of countries) { %>

        <tr>
          <td>
            <div class="avatar">
              <div class="rounded h-24 min-w-40 shadow-xl">
                <img src="<%= country.flag %>" alt="<%= country.name %>" />
              </div>
            </div>
          </td>
          <td class="text-center">
            <%= country.name %> |
            <a
              class="btn btn-primary btn-sm"
              href="/un/countries?region=<%= country.Region.name %>"
              ><%= country.Region.name %></a
            >
          </td>
          <td class="text-center">
            <span class="font-bold">Population:</span> <%=
            country.population.toLocaleString() %> -
            <span class="font-bold">Capital:</span>
            <%= country.capital %>
          </td>
          <td class="text-center">
            <a
              class="btn btn-primary btn-sm"
              href="/un/countries/<%= country.a2code %>"
              >Details</a
            >
          </td>
        </tr>
        <% } %>
      </table>
    </div>

    <% } else { %>
    <h1>No Countries Found!</h1>
    <% } %>
  </body>
</html>
